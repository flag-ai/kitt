# Universal parameter names mapped to engine-specific names
parameter_mapping:
  temperature:
    vllm: temperature
    llama_cpp: temperature
    ollama: temperature
  top_p:
    vllm: top_p
    llama_cpp: top_p
    ollama: top_p
  top_k:
    vllm: top_k
    llama_cpp: top_k
    ollama: top_k
  max_tokens:
    vllm: max_tokens
    llama_cpp: max_tokens
    ollama: num_predict
